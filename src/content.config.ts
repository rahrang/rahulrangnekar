import { categoryCollection } from './content/category.config';
import { postCollection } from './content/post.config';

export const collections = {
  categories: categoryCollection,
  posts: postCollection,
};
